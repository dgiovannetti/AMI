# Builds AMI 3.x from 3.0/ (Windows + macOS). Does not attach to Releases — use release-3.0.yml on tags.
name: Build AMI

on:
  push:
    branches: [main, master]
    paths:
      - "3.0/**"
      - ".github/workflows/build.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "3.0/**"
      - ".github/workflows/build.yml"
  workflow_dispatch:

jobs:
  test:
    name: Pytest (3.0)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "3.0"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libgl1 libglib2.0-0 libdbus-1-3 libxkbcommon0 libfontconfig1
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest
      - name: Run tests
        env:
          PYTHONPATH: src
          QT_QPA_PLATFORM: offscreen
        run: python -m pytest tests -q

  build-windows:
    name: Build for Windows (3.0)
    needs: test
    runs-on: windows-latest
    defaults:
      run:
        working-directory: "3.0"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      - name: Build executable
        run: python build.py
      - uses: actions/upload-artifact@v4
        with:
          name: AMI-Windows-3.0-Package
          path: 3.0/dist/AMI-Package/
          retention-days: 30

  build-macos:
    name: Build for macOS (3.0)
    needs: test
    runs-on: macos-latest
    defaults:
      run:
        working-directory: "3.0"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      - name: Build executable
        run: python build.py
      - uses: actions/upload-artifact@v4
        with:
          name: AMI-macOS-3.0-Package
          path: 3.0/dist/AMI-Package/
          retention-days: 30
