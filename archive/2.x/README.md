# AMI 2.x (frozen)

This tree is the last 2.1.2 line. It is kept for history. Do not extend it, do not ship it, and do not open pull requests against it.

Active development is **AMI 3.x** in [`../../3.0/`](../../3.0/). Run that with `python run.py` from `3.0/`.
